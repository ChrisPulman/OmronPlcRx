<Window x:Class="OmronPlcRxDashboard.Views.AddTagDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Add Tag" Height="250" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name:" VerticalAlignment="Center" />
        <TextBox Grid.Column="1" Margin="6,4" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Text="Address:" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="6,4" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Text="Type:" VerticalAlignment="Center" />
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="6,4" ItemsSource="{Binding AllowedTypes}" SelectedItem="{Binding SelectedType}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Text="{Binding ValidationMessage}" Foreground="Red" Margin="0,4" />

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Margin="4" Padding="12,4" IsDefault="True" IsEnabled="{Binding CanAccept}" Click="Ok_Click" />
            <Button Content="Cancel" Margin="4" Padding="12,4" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
